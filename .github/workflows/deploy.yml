name: Deploy CINK (DISABLED - use main.yml instead)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. Deployments are handled by main.yml"
